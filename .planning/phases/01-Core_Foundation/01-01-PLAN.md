---
phase: 01-Core_Foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devices/src/layouts.rs
  - crates/devices/src/gilrs_backend.rs
  - crates/devices/src/identification.rs
autonomous: true
requirements:
  - F1
  - F7
user_setup: []

must_haves:
  truths:
    - "Physical gamepads enumerate output pins for rumble, RGB, and adaptive trigger signals"
    - "Unsupported devices gracefully expose an empty output list instead of failing enumeration"
    - "PhysicalDevice.outputs contains correct DevicePin metadata for DualShock 4, DualSense, Xbox/XInput, and Switch Pro"
  artifacts:
    - path: "crates/devices/src/layouts.rs"
      provides: "output pin layouts for supported controllers"
      contains: "outputs_for(kind: ControllerKind)"
    - path: "crates/devices/src/gilrs_backend.rs"
      provides: "PhysicalDevice enumeration and output pin attachment"
      contains: "PhysicalDevice { ... outputs: layouts::outputs_for(kind), ... }"
  key_links:
    - from: "gilrs device enumeration"
      to: "PhysicalDevice.outputs"
      via: "layouts::outputs_for(kind)"
      pattern: "outputs_for(kind)"
    - from: "ControllerKind detection"
      to: "output pin selection"
      via: "ControllerKind::detect"
      pattern: "ControllerKind::detect"
---

<objective>
Extend device enumeration so supported physical controllers advertise output pins for rumble, RGB lighting, and adaptive triggers, while preserving backward compatibility with existing patch files and fallback behavior for unsupported hardware.

Purpose: Provide the underlying device capability metadata required by output modules and automatic routing.

Output: updated device pin layouts and enumeration handling in `crates/devices`.
</objective>

<execution_context>
@.planning/phases/01-Core_Foundation/01-CONTEXT.md (D-01, D-05)
@crates/devices/src/layouts.rs
@crates/devices/src/gilrs_backend.rs
@crates/devices/src/identification.rs
</execution_context>

<context>
Existing device enumeration builds `PhysicalDevice` in `crates/devices/src/gilrs_backend.rs` and uses `layouts::outputs_for(kind)` to populate output pins. `DeviceBackend::send` is already present but only fulfills PlayStation devices via `GyroManager`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define controller-specific output pin sets</name>
  <files>crates/devices/src/layouts.rs</files>
  <action>
Expand `layouts.rs` output pin tables so that supported controller kinds expose phase-1 feedback pins:
- `DualShock4`: `rumble_strong`, `rumble_weak`, `lightbar_r`, `lightbar_g`, `lightbar_b`
- `DualSense`: same as DS4 plus `haptic_l`, `haptic_r`, `adaptive_trigger_l`, `adaptive_trigger_r`
- `XInput`: `rumble_strong`, `rumble_weak`
- `SwitchPro`: `hd_rumble_l`, `hd_rumble_r`
- `Generic`: preserve the existing `gamepad::standard_outputs()` fallback and expose no output pins beyond common rumble support if unsupported.

Keep the auto-map output pin `automap_out` in every controller's `outputs_for()` result.
  </action>
  <verify>
    <automated>cd c:\Users\xiso\OneDrive\myrepos\FlexInput; cargo check --package flexinput-devices 2>&1 | grep -E "error\[" || echo "Build OK"</automated>
  </verify>
  <done>controller output pin sets compile and include the required feedback pins for supported controller kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure enumerated PhysicalDevices populate outputs safely</name>
  <files>crates/devices/src/gilrs_backend.rs</files>
  <action>
Update `GilrsBackend::enumerate()` so each `PhysicalDevice` sets `outputs: layouts::outputs_for(kind)` and preserves existing input pin behavior.

Add a fallback path for `ControllerKind::Generic` so `outputs` is empty or limited to rumble pins if no advanced feedback is available, ensuring enumeration never panics.
  </action>
  <verify>
    <automated>cd c:\Users\xiso\OneDrive\myrepos\FlexInput; cargo check --package flexinput-devices 2>&1 | grep -E "error\[" || echo "Build OK"</automated>
  </verify>
  <done>Enumeration compiles and populates output pins for supported controllers while preserving graceful fallback for generic devices.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for output pin enumeration and graceful fallback</name>
  <files>crates/devices/tests/output_enumeration.rs</files>
  <action>
Add tests that exercise `layouts::outputs_for(kind)` and `GilrsBackend::enumerate()` via mocked controller kinds or helper wrappers if supported.

Tests should include:
- `DualShock4` exposes rumble and lightbar output pins
- `DualSense` exposes adaptive trigger pins
- `XInput` exposes rumble pins only
- `Generic` returns an empty or fallback output list without panic

If `GilrsBackend` cannot be directly instantiated for headless tests, cover the helper table functions in `layouts.rs`.
  </action>
  <verify>
    <automated>cd c:\Users\xiso\OneDrive\myrepos\FlexInput; cargo test --package flexinput-devices --test output_enumeration 2>&1 | grep -E "test result:|FAILED|passed"</automated>
  </verify>
  <done>Output pin enumeration tests pass and catch regressions in controller feedback layout definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device enumeration → patch metadata | Output pin lists feed graph routing decisions; malformed output pin metadata can break user mappings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | layouts.rs output tables | mitigate | Keep canonical pin IDs stable and use compile-time constants for supported pins |
| T-01-02 | Denial of Service | unsupported controller kind | accept | Generic controllers return safe empty/fallback output lists without panicking |
| T-01-03 | Information Disclosure | controller metadata | accept | Only device-kind metadata is exposed; no user-sensitive data returned |

</threat_model>

<verification>
- [ ] DualShock4 enumerates rumble and lightbar outputs
- [ ] DualSense enumerates adaptive trigger outputs
- [ ] XInput enumerates rumble outputs only
- [ ] Generic devices enumerate safely with no output crash
- [ ] tests for output enumeration pass
</verification>

<success_criteria>
1. Supported controller kinds expose correct output pin metadata.
2. Physical device enumeration remains stable and backward compatible.
3. Unsupported devices do not fail enumeration.
4. Output pin layout tests compile and pass.
</success_criteria>

<output>
Create `.planning/phases/01-Core_Foundation/01-01-PLAN-SUMMARY.md`
</output>
